# ── 비밀/환경 변수 (절대 커밋 금지) ──
.env
.env.*
!.env.example

# ── 파이썬 부산물 ──
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# ── 런타임 캐시·로그 ──
.cache/
*.log

# ── OS ──
.DS_Store
Thumbs.db

# ── Office 임시 잠금 파일 (~$로 시작) ──
~$*

# ── 애널리스트 리포트 PDF (대용량, 저작권) ──
data/analyst_reports/*.pdf
data/analyst_reports/*.PDF
